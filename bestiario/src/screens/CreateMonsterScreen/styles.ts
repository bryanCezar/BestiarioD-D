import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    container: { flex: 1, padding: 16, backgroundColor: "#111" },
    label: { color: "cyan", marginBottom: 4, marginTop: 12 },
    input: {
        backgroundColor: "#222",
        color: "purple",
        padding: 8,
        borderRadius: 4,
    }
});